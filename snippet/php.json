{
    "dd": {
        "prefix": ["dd"],
        "body": [
            "$LINE_COMMENT @TODO: <${VIM:system('git config --get user.name | xargs echo -n')}: $CURRENT_DATE-$CURRENT_MONTH-$CURRENT_YEAR> remove this debug code",
            "dd(${0});"
        ],
        "description": "Print dd command"
    },
    "dump": {
        "prefix": ["dump"],
        "body": [
            "$LINE_COMMENT @TODO: <${VIM:system('git config --get user.name | xargs echo -n')}: $CURRENT_DATE-$CURRENT_MONTH-$CURRENT_YEAR> remove this debug code",
            "dump(${0});"
        ],
        "description": "Print dump"
    },
    "/**": {
        "prefix": ["/**"],
        "body": [
            "/**",
            "*",
            "*/"
        ],
        "description": "Block comment"
    }
}
